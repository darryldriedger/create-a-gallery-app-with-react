import React from 'react';
import Container from '../Container';

const Coffee = props => {
    return(
      <div>
          <Container subject= "Coffee" />
      </div>
);
}
Coffee.propTypes = {}
export default Coffee;
